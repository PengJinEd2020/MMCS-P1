def read_data_monthly(path, method='all_data(pred_only)'):
    ###
    if method == 'all_data':
        return station_list, array(day,stations,value=end_point),
        array(day,value=num_of_flow), array(flow,value=duration)
    elif method == 'pred_only'
        return station_list, array(day,stations,value=end_point)


# read_data_monthly(path, method='pred_only')
